<div class="container">
  <h1>Save any kind of movies</h1>
  <ul>
    <% @lists.each do |list| %>
      <li><%= list.name %></li>
      <p><%= link_to "details", list_path(list) %></p>
    <% end %>
  </ul>
  <hr>
  <%= link_to "Create new list", new_list_path %>
</div>
